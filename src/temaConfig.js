import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  // palette: {
  //   amarillo: amber
  // }
})
// theme.palette.amarillo = theme.palette.augmentColor(theme.palette.amarillo);

export default theme